# Development / CI only — not installed by the Pinokio launcher.
#
# The test suite stubs Gradio, yt-dlp, Transformers and huggingface_hub, so
# only the packages the tested logic is actually written against are needed.
# Install Torch from the CPU index to keep CI small:
#
#   pip install torch==2.7.0 --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements-dev.txt

pytest>=8.0.0
ruff>=0.6.0
numpy>=1.26.0
