# Input spec only. The launcher installs app/requirements.lock.txt, the
# resolved set; regenerate it with `python tools/relock.py` after editing this
# file, otherwise changes here have no effect on an install.

# PyTorch installed via torch.js (Pinokio)

# Transformers >=5.4 for Cohere Transcribe (CohereAsrForConditionalGeneration)
transformers>=5.4.0,<6.0.0
accelerate>=1.0.0
safetensors>=0.6.0
sentencepiece
protobuf

# Web UI
gradio>=5.0.0,<7.0.0
gradio-client>=2.0.0

# YouTube → MP3
imageio-ffmpeg
yt-dlp

# Audio (Cohere ASR)
librosa>=0.10.0
soundfile>=0.12.0
scipy>=1.10.0

# TranslateGemma + utilities
pillow>=10.0.0
requests>=2.31.0
numpy>=1.26.0
sympy>=1.13.0
hf-transfer>=0.1.4
tqdm>=4.65.0
huggingface_hub>=0.25.0

# OmniVoice is installed by install.js/update.js after torch, pinned and with
# --no-deps: its requirements (torch>=2.4, torchaudio>=2.4, transformers>=5.3.0)
# would let the resolver replace the platform Torch that torch.js installed.
# Its runtime dependencies that are not already listed above follow, so the
# lock still covers them. Keep the pinned version in install.js and update.js
# in sync.
tensorboardx
webdataset
pydub
